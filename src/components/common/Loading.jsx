import React from 'react';

const Loading = () => {
    return (
        <div className = "container h-100 align-middle">
            <i className="fa fa-spinner align-middle">Loading</i>
        </div>
      );
}
 
export default Loading;