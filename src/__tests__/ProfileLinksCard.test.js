import React from 'react';
import { shallow } from 'enzyme';

it('verifying the ProfileLinksCard component structure', () => {
  // const component = shallow(<ProfileLinksCard
  //   data={['www.google.com']}
  //   canEdit={false}
  //   label={'ask'}
  //   handleProfileLinks={jest.fn()}
  //   collection={[{ type: 'author', family: 'adsf', given: 'asdfad' }]}
  // />);
  // expect(component.find('.card-title').length).toBe(1);
  // expect(component.find('.fa-trash').length).toBe(0);
  // expect(component.find('card-subtitle').length).toBe(0);
  // expect(component.find('card-text').length).toBe(0);
  // expect(component.find('label col-2').length).toBe(0);
  // expect(component.find('card border-secondary w-100').length).toBe(0);
  // expect(component.find('form-group row').length).toBe(0);
  // expect(component.find('form-control col-12').length).toBe(0);
  // expect(component.find('col-8').length).toBe(0);
});

it('verifying the ProfileLinksCard component structure', () => {
  // const component = shallow(<ProfileLinksCard
  //   data={['www.google.com']}
  //   canEdit={true}
  //   label={'ask'}
  //   handleProfileLinks={jest.fn()}
  //   collection={[{ type: 'author', family: 'adsf', given: 'asdfad' }]}
  // />);
  // expect(component.find('.card-title').length).toBe(1);
  // expect(component.find('.fa-trash').length).toBe(1);
  // expect(component.find('card-subtitle').length).toBe(0);
  // expect(component.find('card-text').length).toBe(0);
  // expect(component.find('label col-2').length).toBe(0);
  // expect(component.find('card border-secondary w-100').length).toBe(0);
  // expect(component.find('form-group row').length).toBe(0);
  // expect(component.find('form-control col-12').length).toBe(0);
  // expect(component.find('col-8').length).toBe(0);
});

it('verifying the ProfileLinksCard component structure', () => {
  // const component = shallow(<ProfileLinksCard
  //   data={['www.facebook.com', 'www.gmail.com', 'www.twitter.com']}
  //   canEdit={true}
  //   label={0}
  //   handleProfileLinks={jest.fn()}
  //   collection={[{ type: 'author', family: 'adsf', given: 'asdfad' }]}
  // />);
  // expect(component.find('.card-title').length).toBe(1);
  // expect(component.find('.fa-trash').length).toBe(3);
  // expect(component.find('card-subtitle').length).toBe(0);
  // expect(component.find('card-text').length).toBe(0);
  // expect(component.find('label col-2').length).toBe(0);
  // expect(component.find('card border-secondary w-100').length).toBe(0);
  // expect(component.find('form-group row').length).toBe(0);
  // expect(component.find('form-control col-12').length).toBe(0);
  // expect(component.find('col-8').length).toBe(0);
});

it('verifying the ProfileLinksCard component structure', () => {
  // const component = shallow(<ProfileLinksCard
  //   data={['www.gmail.com', 'www.twitter.com']}
  //   canEdit={true}
  //   label={1}
  //   handleProfileLinks={jest.fn()}
  //   collection={[{ type: 'author', family: 'adsf', given: 'asdfad' }]}
  // />);
  // expect(component.find('.card-title').length).toBe(1);
  // expect(component.find('.fa-trash').length).toBe(2);
  // expect(component.find('card-subtitle').length).toBe(0);
  // expect(component.find('card-text').length).toBe(0);
  // expect(component.find('label col-2').length).toBe(0);
  // expect(component.find('card border-secondary w-100').length).toBe(0);
  // expect(component.find('form-group row').length).toBe(0);
  // expect(component.find('form-control col-12').length).toBe(0);
  // expect(component.find('col-8').length).toBe(0);
});
